#$include <stdio.h>

/**
 * main - prints all combination of single digits
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (a = 0; a < 10; a++)
	{
		putchar(a + '0');
		if (a < )
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
